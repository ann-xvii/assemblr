require 'rails_helper'

RSpec.describe RelationshipsController, type: :controller do
	it "enforces that create action requires logged in user to follow another user"
	it "enforces that destroy action requires a logged in user"

end
